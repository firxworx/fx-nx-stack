/** Default port to listen for requests if one is not specified via the environment. */
export const DEFAULT_PORT = 3333

/** Base path of this API. A path other than '/' may be required if this API is not deployed to the apex of a domain/subdomain. */
export const DEFAULT_BASE_PATH = '/'

/** API version (e.g. 'v1') to use in REST URL path. Refer to the global prefix set in `main.ts`. */
export const DEFAULT_API_VERSION = 'v1'
